import { Routes } from '@angular/router';
import { JobsControlPanelComponent } from './jobs-control-panel.component';

export const routes: Routes = [
	{
		path: '',
		component: JobsControlPanelComponent
	}
];
